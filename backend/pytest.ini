[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
testpaths = tests
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning:starlette.*:
    ignore::PendingDeprecationWarning:starlette.*:
markers =
    mongodb: marks tests as MongoDB integration tests